{
 "verdict": "corroborated",
 "confidence": "medium",
 "reasoning": "config.py defines PROVIDER_LABELS with exactly the five providers named in the ticket (openai_compat, anthropic, ollama, github_copilot, codex) and active_provider guards against unknown providers so \"the app never tries to build an unknown provider\", implying a build step keyed on provider name. The verified lookups confirm providers/factory.py and state.py::AppContext.build_provider_for exist in this repository, and core/routing/README.md describes AppProbeClient wrapping AppContext.build_provider_for. The factory body itself is not shown, so the instantiation logic is inferred rather than read.",
 "evidence": [
  {
   "file": "config.py",
   "symbol": "PROVIDER_LABELS",
   "why": "Enumerates exactly the five providers the ticket names as instantiable."
  },
  {
   "file": "config.py",
   "symbol": "AppConfig.active_provider",
   "why": "Migrates unsupported values so the app never tries to build an unknown provider, implying a name-keyed provider build path."
  },
  {
   "file": "config.py",
   "symbol": "AppConfig.provider_conf",
   "why": "Returns the per-provider config dict that a factory would consume to instantiate a provider."
  },
  {
   "file": "core/routing/README.md",
   "symbol": "",
   "why": "States AppProbeClient wraps AppContext.build_provider_for, which already handles TLS trust, 429 retry and gateway — i.e. a central provider construction point."
  }
 ],
 "status_conflict": false,
 "_usage": {
  "input_tokens": 13025,
  "output_tokens": 675,
  "model": "claude-opus-5"
 }
}