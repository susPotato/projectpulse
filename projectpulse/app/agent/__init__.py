"""Free-form chat - the one generative surface in this app with no gate.

Everything under `intelligence/` and `narration/` exists to keep a model away
from inventing a finding or a figure (see `narration/client.py`'s three
fences). This is the opposite kind of feature on purpose: a PM asking an open
question gets an open answer, clearly a different surface from Insight and
Risk - never the same page, never the same trust claim.
"""

from __future__ import annotations
